import numpy as np

list = [2, 2, 3, 1, 5]
print(np.average(list)) # 평균
print(np.median(list)) # 중앙값
print(np.argmax(list)) # max 값
print(np.argmin(list)) # min 값
print(np.var(list)) # 분산
print(np.std(list)) # 표준편차